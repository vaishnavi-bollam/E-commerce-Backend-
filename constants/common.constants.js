const sortingConsts = {
    PRICE_HIGH : "PRICE_HIGH",
    PRICE_LOW : "PRICE_LOW"
}

//Sets the sort order. If an object is passed, values allowed are asc, desc, ascending, descending, 1, and -1.
const order = {
    ASCENDING:"ascending",
    DESCENDING :"descending"
}

module.exports = {sortingConsts,order}